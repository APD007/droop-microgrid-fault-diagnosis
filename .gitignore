# MATLAB / Simulink build caches - regenerated automatically, never edited
slprj/
cache_w*/
*.slxc
*.asv
*.autosave
*.slx.r20*
*.slx.original

# Superseded partial sweep from the first feature schema (true RMS + DC offset,
# no fundamental phasors). Kept locally as insurance until the current sweep
# completes, then deleted. Never part of the deliverable.
data/raw_v1/

# Scratch
data/.partial.csv

# OS noise
Thumbs.db
desktop.ini
.DS_Store

# Excel lock files
~$*.xlsx

# Python bytecode
__pycache__/
*.pyc

# Scratch working files
scratch/
